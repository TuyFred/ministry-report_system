# Backend environment variables for Production (Render)
PORT=5000

# Secrets (IMPORTANT: Set these in Render Dashboard -> Environment Variables)
JWT_SECRET=your_jwt_secret_here
ADMIN_SECRET_KEY=your_admin_secret_here

# Database Connection
# For Render PostgreSQL: Use the Internal Database URL from Render Dashboard
# Format: postgresql://user:password@host:5432/database
DATABASE_URL=postgresql://postgres:YOUR_DB_PASSWORD@YOUR_DB_HOST:5432/postgres?sslmode=require

# Frontend URL (for CORS - optional, already configured in code)
CLIENT_URL=https://ministry-report-system.vercel.app

# Optional defensive setting for pg
PGSSLMODE=require
